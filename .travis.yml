language: go

go:
  - 1.10
  - 1.11
  - 1.12
  - master

before_install:
  - go get -u golang.org/x/lint/golint

install:
  - go build .

script:
  - golint -set_exit_status ./...
  - [ -z "$(gofmt -d .)" ]
  - go vet ./...
  - go test -v ./...
